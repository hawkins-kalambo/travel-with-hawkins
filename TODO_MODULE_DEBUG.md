# TODO_MODULE_DEBUG (Travel with Hawkins)

## Goal
Identify root causes and minimal fixes for:
- Turbopack build errors around `lib/bookingUtils.ts`
- `status` becoming `undefined`
- Frontend receiving HTML instead of JSON

## Steps
1. Confirm if `lib/bookingUtils.ts` contains any `
